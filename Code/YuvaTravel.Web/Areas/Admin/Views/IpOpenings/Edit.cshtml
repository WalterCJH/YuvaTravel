@model IpOpeningCreateOrEdit

@{
    ViewBag.Title = StrText.Edit + StrText.IpOpenings;
}

@section breadcrumb{
    <cus-breadcrumb category="@StrText.WebPageInfo"
                    resource="@StrText.IpOpenings"
                    current="@ViewBag.Title" />
}

<form asp-action="@nameof(StrText.Edit)" method="post">
    <input type="hidden" asp-for="IpOpeningId" />

    <!-- Main content -->
    <section class="content">
        <div class="row">
            <div class="col-12">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-6">
                <div class="card card-primary">
                    <div class="card-header">
                        <h3 class="card-title">@StrText.Generally</h3>
                        <div class="card-tools">
                            <button type="button" class="btn btn-tool" data-card-widget="collapse" title="Collapse">
                                <i class="fas fa-minus"></i>
                            </button>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="form-group">
                            <cus-checkbox asp-for="IsActive" />
                        </div>
                        <div class="form-group">
                            <label asp-for="IpAddress" class="control-label col-md-12"></label>
                            <input asp-for="IpAddress" class="form-control" />
                            <span asp-validation-for="IpAddress" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="Memo" class="control-label col-md-12"></label>
                            <textarea asp-for="Memo" class="form-control" rows="6"></textarea>
                            <span asp-validation-for="Memo" class="text-danger"></span>
                        </div>
                    </div>
                    <!-- /.card-body -->
                </div>
                <!-- /.card -->
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                <a asp-action="@nameof(StrText.Index)" class="btn btn-secondary">@StrText.ReturnIndex</a>
                <input type="submit" value="@StrText.Save" class="btn btn-success float-right">
            </div>
        </div>
    </section>
</form>

@section Scripts {
    }
